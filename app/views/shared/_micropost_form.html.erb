<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Escribe tu mapost" %>
  </div>
  <%= f.submit "MaPost", class: "btn btn-large btn-primary" %>
<% end %>
